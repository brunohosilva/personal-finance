import {StyleSheet} from 'react-native';

const styles = StyleSheet.create({
  container: {
    width: '100%',
    marginTop: 20,
  },
  textInput: {
    backgroundColor: '#FFF',
    padding: 10,
    borderRadius: 8,
    minHeight: 40,
  },
  errorText: {
    marginTop: 5,
    color: 'red',
    fontStyle: 'italic',
    fontWeight: 'bold'
  },
});

export {styles};
